部分工程中，出现下行64字节限制（不能超出64字节），是因为sx1276-board.h文件中的以下代码：
{ MODEM_LORA, REG_LR_PAYLOADMAXLENGTH, 0x40 },\


部分正确工程中，已设置为：
{ MODEM_LORA, REG_LR_PAYLOADMAXLENGTH, 0xFF },\

这部分需详细确认。

PS：根据测试，如果接收方配置了0x40，则只能接收64字节以内的数据包；
而发送方配置何值，无论发送字节长度多少，都是能完整发送出去的。即该配置限制了接收方接收字节数。